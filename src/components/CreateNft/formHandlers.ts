import axios from "axios";

const PINATA_KEY = process.env.NEXT_PUBLIC_PINATA_KEY;
const PINATA_SECRET = process.env.NEXT_PUBLIC_PINATA_SECRET;

// export const handleSubmitUID = async (data, contract) => {
//   // ... (lógica para manejar el envío del formulario UID)
// };

// export const handleSubmitCollection = async (data, contract) => {
//   // ... (lógica para manejar el envío del formulario de colección)
// };

// export const handleSubmitNFT = async (data, contract, allCollections) => {
//   // ... (lógica para manejar el envío del formulario NFT)
// };
